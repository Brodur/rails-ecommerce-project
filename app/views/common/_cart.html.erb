<details>
  <summary>Cart 🛒</summary>
  <% if @cart.empty? %>
    <p>Cart is empty!</p>
  <% else %>
    <ul>
      <% @cart.each do |product, quantity| %>
        <li>
          <%= quantity %> x <%= product.name %> <%= link_to ' ➖ from 🛒', remove_from_cart_path(product.id), method: :delete %>

          <%= form_with(url: update_cart_quantity_path(product.id), method: "patch") do %>
            <%= label_tag(:quantity, "Quantity:") %>
            <%= number_field_tag 'quantity', quantity.to_s, min: 1, max: product.on_hand %>
            <%= submit_tag("✅ Update Quantity") %>
          <% end %>
        </li>
      <% end %>
    </ul>
  <% end %>
</details>