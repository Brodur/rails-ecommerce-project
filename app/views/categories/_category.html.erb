<hr/>
<h3><%= category.name %></h3>
<p><%= path_to_string category %></p>
<% if category.children.present? %>
  <h5>Child Categoires:</h5>
  <ul>
    <% category.children.each do |c| %>
      <li><%= path_to_string c %></li>
    <% end %>
  </ul>
<% end %>

<p>Products: <%= category.products.count %></p>

<%= will_paginate category.products.paginate(:page => params[:page]), list_classes: %w(pagination justify-content-end) %>
<% category.products.paginate(:page => params[:page]).each do |product| %>
  <%= render partial: "products/products", locals: {product: product} %>
<% end %>
<%= will_paginate category.products.paginate(:page => params[:page]), list_classes: %w(pagination justify-content-end) %>